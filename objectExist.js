const arr = [
    {a1: "a"},
    {b1: "b"},
    {c1: "c"}
]
const valor = {b: "b"}

const isIncluded = (array, val) => {
    for (let i = 0; i < array.length; i++) {
        
        return "hola"
    }
}

console.log(isIncluded(arr, valor))
console.log(Object.values(arr)[1])
